# Rubrics

One file per *type* of subjective deliverable, created by the `make-rubric`
skill. Each rubric carries its own verifier prompt so a fresh agent can score
a deliverable with zero extra setup.

Reuse before creating: refine an existing rubric rather than forking a
near-duplicate. When the user rejects something a rubric passed, that's a
missing criterion — add it.
